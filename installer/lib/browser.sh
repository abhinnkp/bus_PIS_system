#!/usr/bin/env bash

# Placeholder, heavily documented in abdos-kiosk.sh.in but kept for module structure parity
configure_browser() {
    log_info "Browser configuration will be managed dynamically at runtime via scripts/abdos-kiosk.sh"
}
